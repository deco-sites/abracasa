import Component from "$store/components/search/SearchTitle.tsx";
import type { Props } from "$store/components/search/SearchTitle.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
