import Component from "$store/components/newsletter/NewsletterForm.tsx";
import type { Props } from "$store/components/newsletter/NewsletterForm.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
