import Component from "$store/components/product/AddToCartRedirectButton.tsx";
import type { Props } from "$store/components/product/AddToCartRedirectButton.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
